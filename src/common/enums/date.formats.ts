export enum DateFormats {
    fullDayName = 'dddd',
    shortDate = 'DD MMM',
    fullDate = 'DD MMM YYYY',
    timeOnly = 'HH:mm',
    fullDateTime = 'DD MMM YYYY HH:mm',
    monthYear = 'MMMM YYYY'
}
